{
  "version": 2,
  "buildCommand": "cd furnterra-backend && npm install && npm run build && cd ../furnterra-frontend && npm install && npm run build",
  "installCommand": "npm install",
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs20.x"
    }
  }
}