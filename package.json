{
  "name": "furnterra",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd furnterra-backend && npm install && npm run build",
    "build:frontend": "cd furnterra-frontend && npm install && npm run build",
    "vercel-build": "npm run build:backend && npm run build:frontend"
  }
}