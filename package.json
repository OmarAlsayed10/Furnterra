{
  "name": "furnterra",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:backend": "cd furnterra-backend && npm install && npm run build",
    "vercel-build": "npm run build:backend"
  }
}